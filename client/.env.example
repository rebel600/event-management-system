# Leave empty for local dev — Vite proxies /api to the local server.
# In production set the deployed backend origin, no trailing slash and no /api suffix.
# e.g. https://your-server.up.railway.app
VITE_API_URL=
