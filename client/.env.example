# Leave empty. The client and server are deployed as one service on a single
# origin, so /api resolves against the page's own host in both dev and prod.
# Only set this if you split the frontend onto its own domain, in which case it
# is the backend origin: no trailing slash, no /api suffix.
VITE_API_URL=
